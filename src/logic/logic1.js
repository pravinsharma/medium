if (name != ‘George’ && city != ‘Milan’) {
	// some statements
} else if (name == ‘George’ || city == ‘Milan’) {
	// other statements
} else { // un-reachable code which the compiler will not warn of
	// un reachable code
}
